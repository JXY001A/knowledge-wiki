---
title: 日报 2026-05-23
type: synthesis
tags: [日报]
created: 2026-05-23
updated: 2026-05-23
confidence: high
---

# 日报 2026-05-23

## 今日产出

### 一、项目重构：三 repo 统一工程化

- 将 `wiki-mcp-server` + `wecom-bot-webhook` + `knowledge-wiki` 三个独立仓库合并为单一 Python 项目
- 建立 `src/knowledge_wiki/` 模块化包结构（28 个模块，~2200 行），拆分两个单文件脚本（1148 行 + 392 行）
- 创建 `pyproject.toml` 统一依赖管理，`pydantic-settings` 集中环境变量
- `deploy/setup.sh` 实现 DevMechin 一键部署脚本
- 敏感信息从 systemd 明文 `Environment=` 迁移到 gitignored `.env` 文件
- **安全验证**：Git 历史零泄露，`.env` 从未进入版本控制

### 二、Phase 2 Agent 技能体系实现

- 重写 `skill/planner.py` 执行引擎：`_load_impl()` 通过 `importlib` 动态加载技能实现，支持 sync/async
- 创建 5 个核心技能，每个含 `skill.json` + `SKILL.md` + `impl.py`：
  - `search-wiki` — 关键词搜索，Tier 1，local
  - `query-knowledge` — MCP 检索 + LLM 综合回答，Tier 1，local（含四级容错链）
  - `ingest-article` — URL 摄取完整流水线，Tier 1，deepseek
  - `lint-wiki` — 健康检查报告，Tier 2，local
  - `save-note` — 文本保存（默认回退），Tier 1，local
- 改造 `webhook/process.py`：`process_message()` 从硬编码三路 `if/elif/else` 分发改为 Skill 引擎路由
- MCP 新增 `skill-list` / `skill-execute` 工具
- **测试**：32 项全通过（新增 12 项技能系统测试）

### 三、DevMechin 部署

- 创建 `.env` 文件（密钥从旧 systemd 迁移）
- 创建 `.venv` + `pip install -e .`
- 修复 `FastMCP.run()` 不接受 `host/port` 参数的兼容性问题
- 安装新 systemd 单元 → 停旧服务 → 启新服务 → 验证
- 清理旧仓库 `wiki-mcp-server/` 和 `wecom-bot-webhook/`
- MCP Server :9300 + Webhook :9400 均正常运行

### 四、架构文档

- 新建 `知识库系统工程化架构.md`（123 行 synthesis）
- 新建 `2026-05-23 Phase 2 实现详细流程图.md`（~400 行，11 章节，含 Mermaid 图）
- 多次修复 Mermaid 渲染问题（`()` / `→` / `✅` / `{}` / `├└` 在 Obsidian 中的兼容性）

### 五、知识与模型盘点

- 整理 DevMechin 5 个本地模型清单、GPU 显存分布、调用函数对照表
- 文档化模型选型决策表（qwen2.5:3b vs llama3.2:1b vs deepseek-v4-pro 六维度对比）
- 建立 qwen3-vl:8b 截图诊断流程（截图 → scp → Ollama API → 分析结果）

## 代码变更

14 个提交：

| Commit | 内容 |
|--------|------|
| `synthesis: 日报 2026-05-22` | 前一天工作总结 |
| `ingest: Claude Code 长期记忆系统` | 概念页 |
| `ingest: Agent技术范式演变` | 概念页完善 |
| `cleanup: 清除 raw/ 无效文件` | 25 个文件清理 |
| `synthesis: DeepSeek API 驱动 ingest 架构` | Phase 1 文档 |
| `synthesis: 知识库系统工程化架构文档` | 重构总结 |
| `fix: FastMCP.run() 参数修复` | 部署兼容性 |
| `feat: Phase 2 Agent 技能体系` | **核心提交** — 5 技能 + Skill 路由 |
| `synthesis: 更新知识库系统工程化架构` | Phase 2 详情 |
| `synthesis: Phase 2 实现详细流程图` | 初始版 ~400 行 |
| `synthesis: 补充流程图 — 模型清单` | +5 章节 |
| `fix: 删除重复容错链` + 3 个渲染修复 | 流程图打磨 |
| `fix: 拓扑图连线补全` × 2 | DeepSeek/qwen3-vl 连线 |

## 知识积累

- 新建 wiki 页面：
  - `知识库系统工程化架构`
  - `2026-05-23 Phase 2 实现详细流程图`
  - `Claude Code 长期记忆系统`（概念页）
  - `Agent Skills体系`（概念页）
  - `Agentic Workflow`（概念页）
  - `上下文工程`（概念页）
  - `DeepSeek API 驱动 ingest — 架构与实现`（综合分析）
- 更新 `AI 自进化知识系统 — 建设路线图`、`知识库系统全链路架构` 等

## 经验沉淀

- **分层设计验证**：模块化的 `wiki/` 和 `llm/` 基础层使技能实现可以一行调用现有能力，5 个 `impl.py` 平均 ~40 行
- **Mermaid 踩坑**：Obsidian Mermaid 对 `()` `{}` `→` `✅` `├└` 敏感，会触发 "Unsupported markdown: list"
- **FastMCP API 差异**：`run()` 不接受 `host/port`，需在构造函数中传入
- **部署模式**：`.env` + `EnvironmentFile` 替代 systemd 内联密钥

## 次日计划

（留空，由人工填写）
