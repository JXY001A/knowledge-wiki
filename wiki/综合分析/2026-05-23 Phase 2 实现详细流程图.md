---
title: "2026-05-23 Phase 2 实现详细流程图"
type: synthesis
tags: [架构, 流程图, Phase2, Agent, 技能体系, 工程化]
created: 2026-05-23
updated: 2026-05-23
sources:
  - "[[知识库系统工程化架构]]"
  - "[[AI 自进化知识系统 — 建设路线图]]"
  - "[[知识库系统全链路架构]]"
confidence: high
---

> Phase 2 完成后 knowledge-wiki 系统的完整流程图：全链路拓扑、消息处理、Skill 引擎、URL 摄取、知识查询、注册表加载、阶段对比，以及 DevMechin 本地模型清单、Ollama 查询容错链、模型路由决策与选型对照。

## 一、全链路部署拓扑

```mermaid
graph TB
    subgraph Mac["Mac（开发端）"]
        Obsidian["Obsidian<br/>wiki 编辑"]
        ClaudeCode["Claude Code<br/>AI 编程"]
        GitPush["git push"]
    end

    subgraph GitHub["GitHub 私有仓库"]
        Repo["knowledge-wiki<br/>代码 + wiki 内容"]
    end

    subgraph DevMechin["DevMechin（运行端）<br/>Ubuntu 24.04 · RTX 4080 · i7-14700KF"]
        subgraph Services["systemd 服务"]
            MCP["wiki-mcp.service<br/>:9300<br/>FastMCP + Streamable HTTP"]
            Webhook["wecom-webhook.service<br/>:9400<br/>Flask"]
            Frpc["frpc.service<br/>隧道客户端"]
        end

        subgraph Runtime["运行时"]
            Venv[".venv/<br/>Python 3.12"]
            Ollama["Ollama<br/>qwen2.5:3b<br/>:11434"]
            Env[".env<br/>密钥（不进 Git）"]
        end

        subgraph Code["代码"]
            Src["src/knowledge_wiki/<br/>28 模块"]
            Skills["skills/<br/>5 技能"]
        end
    end

    subgraph Cloud["阿里云上海"]
        Frps["frps<br/>8.133.175.201<br/>:9300, :9400"]
    end

    subgraph External["外部"]
        Wechat["企业微信<br/>回调 → :9400"]
        DeepSeek["DeepSeek API<br/>api.deepseek.com"]
    end

    Obsidian --> GitPush
    ClaudeCode --> GitPush
    GitPush --> Repo
    Repo -->|"git pull"| DevMechin

    Env --> Services
    Venv --> MCP
    Venv --> Webhook

    MCP -->|"127.0.0.1:9300"| Frpc
    Webhook -->|"127.0.0.1:9400"| Frpc
    Frpc -->|"frp 隧道"| Frps
    Frps -->|":9300 MCP"| ClaudeCode
    Frps -->|":9400 Webhook"| Wechat

    Webhook -.->|"ingest 时调用"| DeepSeek
    Webhook -.->|"query 时调用"| Ollama
    MCP -.->|"query 时读取"| Repo
```

## 二、企业微信消息处理全链路

```mermaid
flowchart TD
    WX["手机微信"] -->|"用户发消息"| WXServer["企业微信服务端"]
    WXServer -->|"POST XML（AES 加密）"| Frps["阿里云 frps<br/>8.133.175.201:9400"]
    Frps -->|"frp 隧道"| Frpc["DevMechin frpc<br/>forward to 127.0.0.1:9400"]
    Frpc -->|"转发"| Flask["Flask receive_message"]

    Flask -->|"1. 提取加密 XML"| Decrypt["decrypt_msg<br/>AES-256-CBC 解密"]
    Decrypt -->|"2. 明文 XML"| Parse["解析 MsgType<br/>FromUserName<br/>Content"]
    Parse -->|"3. 后台线程"| Thread["threading.Thread<br/>target=process_message"]

    Thread --> Dispatch["process_message<br/>Skill 路由引擎"]

    Dispatch -->|"? + 搜索"| SkillSearch["search-wiki"]
    Dispatch -->|"? + 查询"| SkillQuery["query-knowledge"]
    Dispatch -->|"URL"| SkillIngest["ingest-article"]
    Dispatch -->|"检查 lint"| SkillLint["lint-wiki"]
    Dispatch -->|"其他文本"| SkillSave["save-note（默认回退）"]

    SkillSearch --> Reply["send_markdown<br/>to 企业微信 API"]
    SkillQuery --> ReplyCards["send_template_card<br/>多卡片 + markdown"]
    SkillIngest --> Reply
    SkillLint --> Reply
    SkillSave --> Reply

    Reply --> WXAPI["qyapi.weixin.qq.com<br/>主动消息 API"]
    ReplyCards --> WXAPI
    WXAPI -->|"推送"| WX
```

## 三、Skill 执行引擎内部流程

```mermaid
flowchart TD
    Start["process_message(user_id, text, cb_md, cb_tpl)"] --> Strip["stripped = text.strip()"]
    Strip --> BuildCtx["构建 context 字典<br/>user_id, input_text<br/>send_md, send_tpl callbacks"]

    BuildCtx --> Check{"text 以 ? 开头?"}

    Check -->|"是"| ExtractQ["question = stripped[1:]<br/>ctx['query'] = question"]
    ExtractQ --> MatchQ["match_skill(question)"]

    Check -->|"否"| CheckURL{"is_url(text)?"}
    CheckURL -->|"是"| MatchURL["match_skill(url)"]
    CheckURL -->|"否"| MatchText["match_skill(text)"]

    MatchQ --> Found{"skill 匹配成功?"}
    MatchURL --> Found
    MatchText --> Found

    Found -->|"是"| Exec["execute_skill(skill.name, ctx)"]
    Found -->|"否"| Fallback{"回退链"}

    Fallback -->|"?"| FBQuery["execute_skill('query-knowledge', ctx)"]
    Fallback -->|"URL"| FBIngest["execute_skill('ingest-article', ctx)"]
    Fallback -->|"text"| FBSave["execute_skill('save-note', ctx)"]

    Exec --> LoadSkill["1. find_skill(name)<br/>→ Skill 对象"]
    FBQuery --> LoadSkill
    FBIngest --> LoadSkill
    FBSave --> LoadSkill

    LoadSkill --> LoadImpl["2. _load_impl(skill)<br/>importlib 加载<br/>skills/{name}/impl.py"]
    LoadImpl --> HasImpl{"impl.py 存在?"}

    HasImpl -->|"是"| CallImpl["3. impl.execute(context)"]
    HasImpl -->|"否"| FallbackMD["返回 SKILL.md 文本<br/>+ 提示缺少实现"]

    CallImpl --> Async{"async def execute?"}
    Async -->|"是"| Await["asyncio.run(execute(ctx))"]
    Async -->|"否"| Sync["execute(ctx)"]

    Await --> Result["返回结果文本"]
    Sync --> Result
    FallbackMD --> Result

    Result --> SendBack{"结果非空?"}
    SendBack -->|"是"| SendMD["send_md(user_id, result[:3000])"]
    SendBack -->|"否（技能已回调发送）"| Done["处理完成"]
    SendMD --> Done
```

## 四、URL 摄取完整流水线（ingest-article）

```mermaid
flowchart TD
    URL["用户发送 URL"] --> Notify["send_md('正在提取并分析...')"]

    Notify --> Fetch["1. fetch_url_text(url)"]
    Fetch --> FetchDetail["HTTP GET with User-Agent<br/>extract og:title / h1 / title<br/>strip script/style/nav<br/>cap at 50KB"]
    FetchDetail --> FetchOK{"下载成功?"}

    FetchOK -->|"no"| SaveRef["save_to_inbox: 待摄取链接<br/>git commit + push"]
    SaveRef --> NotifyFail["send_md: 无法下载"]

    FetchOK -->|"yes"| SaveRaw["2. save_to_inbox(raw_text)<br/>raw/收件箱/wecom-url-ts.md"]

    SaveRaw --> DeepSeek["3. call_ingest(raw_text, url)"]
    DeepSeek --> DeepSeekDetail["POST api.deepseek.com<br/>model: deepseek-v4-pro<br/>system: full ingest prompt<br/>temp: 0.1, max_tokens: 4096"]

    DeepSeekDetail --> DeepSeekOK{"LLM 返回 JSON?"}

    DeepSeekOK -->|"no"| NotifyLLMFail["send_md: LLM 分析失败"]

    DeepSeekOK -->|"yes"| ParseJSON["json.loads(response)<br/>parse structured fields:<br/>title, domain, tags, summary<br/>key_points[], notes, quotes<br/>concepts[], related_pages[]"]

    ParseJSON --> BuildSource["4. build_source_page(data, url)<br/>wiki/资料摘要/title.md<br/>generate frontmatter + body"]

    BuildSource --> BuildConcepts["5. build_concept_page(concept)<br/>wiki/概念/name.md<br/>skip if already exists"]

    BuildConcepts --> AppendLog["6. append_ingest_log(data, url, title)<br/>wiki/操作日志.md"]

    AppendLog --> GitPush["7. commit_and_push(msg)<br/>git add -A, commit, push"]

    GitPush --> NotifyOK["send_md: OK 已摄取到知识库<br/>title, domain, summary<br/>plus new concept names"]
```

## 五、知识查询流水线（query-knowledge）

```mermaid
flowchart TD
    Q["用户: ? AI Workflow 是什么"] --> MCP["1. mcp_query(question)"]

    MCP --> MCPDetail["初始化 MCP 会话<br/>POST :9300/mcp → initialize<br/>获取 Mcp-Session-Id<br/>POST tools/call → query"]
    MCPDetail --> MCPOK{"找到结果?"}

    MCPOK -->|"否"| SearchFallback["回退: tools/call → search"]
    SearchFallback --> SearchFallbackOK{"找到?"}
    SearchFallbackOK -->|"否"| NotFound["send_md('暂无相关内容')"]
    SearchFallbackOK -->|"是"| ReadPages["读取匹配页面全文<br/>strip_frontmatter<br/>限制 2000 字/页"]

    MCPOK -->|"是"| StripFM["2. strip_frontmatter(context)<br/>去除 YAML 元数据"]

    ReadPages --> StripFM

    StripFM --> LLM["3. call_json(question, context)"]
    LLM --> LLMDetail["POST :11434/api/chat<br/>model: qwen2.5:3b<br/>system: Output ONLY valid JSON<br/>Format: summary + cards array<br/>num_predict: 2000, temp: 0.1"]

    LLMDetail --> JSONOK{"JSON 解析成功?"}

    JSONOK -->|"否"| KWSearch["回退: keyword_search(question)"]
    KWSearch --> KWSend["send_template_card(第一匹配页面)"]

    JSONOK -->|"是"| ProcessCards["4. 遍历 cards[:8]"]

    ProcessCards --> ForEach["for i, card in enumerate(cards)"]
    ForEach --> Sleep{"i > 0?"}
    Sleep -->|"是"| Wait["time.sleep(0.8)<br/>防止企业微信乱序"]
    Sleep -->|"否"| Normalize["_normalize_details(card.details)<br/>标准化为 [(k,v), ...]"]
    Wait --> Normalize

    Normalize --> ShortSummary{"card.summary 为空?"}
    ShortSummary -->|"是"| CallShort["call_short(title + details)<br/>qwen2.5:3b 一句话摘要"]
    ShortSummary -->|"否"| SendCard["send_template_card(user_id,<br/>title, summary, details,<br/>source_desc='页面名 (i/N)')"]
    CallShort --> SendCard

    SendCard --> MoreCards{"还有卡片?"}
    MoreCards -->|"是"| ForEach
    MoreCards -->|"否"| Overall{"card_data.summary > 100字?"}
    Overall -->|"是"| SendOverall["send_markdown(clean_summary)"]
    Overall -->|"否"| Done["完成"]
    SendOverall --> Done
```

**LLM 调用四级容错链**（内建于 `query-knowledge/impl.py`）：

```
qwen2.5:3b JSON
  └─ 失败 → keyword_search() 直接返回第一匹配页面
  └─ 成功 → 遍历 cards，每张 card.summary 为空时
              └─ qwen2.5:3b call_short() 一句话摘要
              └─ 失败 → 跳过该 card
多卡片发送后 → card_data.summary > 100 字时 send_markdown 补充
```

## 六、技能注册表加载流程

```mermaid
flowchart LR
    subgraph Filesystem["skills/ 文件系统"]
        SA["search-wiki/<br/>  skill.json<br/>  SKILL.md<br/>  impl.py"]
        QA["query-knowledge/<br/>  skill.json<br/>  SKILL.md<br/>  impl.py"]
        IA["ingest-article/<br/>  skill.json<br/>  SKILL.md<br/>  impl.py"]
        LW["lint-wiki/<br/>  skill.json<br/>  SKILL.md<br/>  impl.py"]
        SN["save-note/<br/>  skill.json<br/>  SKILL.md<br/>  impl.py"]
    end

    Filesystem -->|"list_skills()"| Scan["遍历 skills/ 子目录<br/>读取 skill.json<br/>→ Skill 对象列表"]

    Scan --> Tier1["Tier 1（4 技能）<br/>search-wiki, query-knowledge<br/>ingest-article, save-note<br/>→ generate_tier1_context()<br/>Agent 启动时加载"]

    Scan --> Tier2["Tier 2（1 技能）<br/>lint-wiki<br/>→ 意图匹配时按需加载"]

    Tier1 --> Load2["load_skill_body(skill)<br/>→ 读取 SKILL.md 全文<br/>→ LLM context"]
    Tier2 --> Load2

    Load2 --> Load3["load_skill_resources(skill)<br/>→ 读取 scripts/ + references/<br/>→ 执行时按需"]
```

## 七、各阶段对比

| | Phase 0（最初） | Phase 1（基座升级） | Phase 2（当前） |
|---|---|---|---|
| **代码组织** | 3 个独立 repo，2 个单文件脚本 | 统一 `src/knowledge_wiki/` 包 | 不变 |
| **消息分发** | 硬编码 `if/elif/else` | 硬编码 `if/elif/else` | Skill 引擎路由 |
| **技能系统** | 无 | `skill/` 骨架（占位） | 5 技能 + 执行引擎 |
| **LLM 调用** | 散落各处，接口不一 | 统一 `llm/` 包 | 不变 |
| **部署** | systemd 明文密钥 | `.env` + `EnvironmentFile` | 不变 |
| **测试** | 0 | 20 项 | 32 项 |
| **可扩展性** | 加功能 = 加 if/elif 分支 | 加功能 = 加模块 | 加功能 = 加技能目录 |

## 八、模型资源清单

### 本地模型（DevMechin Ollama · RTX 4080 16GB）

| 模型 | 大小 | 用途 | 调用函数 | 参数特征 |
|------|------|------|---------|---------|
| `qwen2.5:3b` | 1.9 GB | **主查询模型** — JSON 结构化输出 | `call_json()` | num_predict=2000, temp=0.1 |
| `qwen2.5:3b` | — | 一句话摘要 | `call_short()` | num_predict=120, temp=0.1 |
| `qwen2.5:3b` | — | 详细 markdown 回答 | `call_detailed()` | num_predict=1500, temp=0.3 |
| `qwen2.5:1.5b` | 986 MB | （备用）轻量中文推理 | — | 已安装，当前未调用 |
| `llama3.2:1b` | 1.3 GB | **最终回退** — 所有模型失败时 | `call_fallback()` | num_predict=200, temp=0.1 |
| `qwen3:4b` | 2.5 GB | （备用）中文通用推理 | — | 已安装，当前未调用 |
| `qwen3-vl:8b` | 6.1 GB | 图片解析（`~/bin/vision`） | — | 独立脚本，非 MCP 链路 |

### 云端模型

| 模型 | 提供商 | 用途 | 调用函数 | 参数特征 |
|------|--------|------|---------|---------|
| `deepseek-v4-pro` | DeepSeek API | **ingest 分析** — URL → wiki 结构化数据 | `call_ingest()` | max_tokens=4096, temp=0.1 |

### GPU 资源占用

```
RTX 4080 SUPER 16GB GDDR6X
├── qwen2.5:3b     ── 1.9 GB  ← 主查询（常驻显存）
├── qwen2.5:1.5b   ── 1.0 GB  ← 备用
├── llama3.2:1b    ── 1.3 GB  ← 回退（常驻显存）
├── qwen3:4b       ── 2.5 GB  ← 备用
├── qwen3-vl:8b    ── 6.1 GB  ← 图片（按需加载）
└── 剩余           ── ~3.2 GB  ← 可用
```

## 九、模型路由决策流程

```mermaid
flowchart TD
    Request["Skill 执行请求"] --> SkillModel{"skill.json<br/>model 字段?"}

    SkillModel -->|"deepseek"| DS["→ DeepSeek API"]
    SkillModel -->|"local"| Local["→ 本地 Ollama"]
    SkillModel -->|"auto"| Auto{"意图含关键词?<br/>ingest/分析/总结/生成"}

    Auto -->|"是（复杂任务）"| DS
    Auto -->|"否（简单任务）"| Local

    DS --> DSDetail["POST api.deepseek.com/v1<br/>deepseek-v4-pro<br/>128K context<br/>¥0.01/篇"]

    Local --> Route{"路由到哪个本地模型?"}

    Route -->|"需要 JSON 输出"| Qwen3B["call_json()<br/>qwen2.5:3b<br/>num_predict=2000"]
    Route -->|"需要一句话摘要"| Short["call_short()<br/>qwen2.5:3b<br/>num_predict=120"]
    Route -->|"需要详细回答"| Detail["call_detailed()<br/>qwen2.5:3b<br/>num_predict=1500"]
    Route -->|"最终回退"| Fallback["call_fallback()<br/>llama3.2:1b<br/>num_predict=200"]

    Qwen3B --> Output["返回结果"]
    Short --> Output
    Detail --> Output
    Fallback --> Output
    DSDetail --> Output
```

## 十、模型与技能的对应关系

```mermaid
graph LR
    subgraph Skills["5 个技能"]
        SW["search-wiki"]
        QK["query-knowledge"]
        IA["ingest-article"]
        LW["lint-wiki"]
        SN["save-note"]
    end

    subgraph Local["本地 Ollama :11434"]
        Qwen["qwen2.5:3b<br/>主查询"]
        Llama["llama3.2:1b<br/>最终回退"]
    end

    subgraph Cloud["云端 API"]
        DS["DeepSeek v4-pro<br/>api.deepseek.com"]
    end

    SW -->|"纯文本搜索"| Direct["直接 grep<br/>无需 LLM"]
    QK -->|"JSON 结构化"| Qwen
    QK -.->|"主模型失败时"| Llama
    IA -->|"内容分析"| DS
    LW -->|"纯代码检查"| Direct
    SN -->|"纯文件保存"| Direct

    style Direct fill:#b2f2bb,stroke:#333
    style Qwen fill:#a5d8ff,stroke:#333
    style Llama fill:#ffd8a8,stroke:#333
    style DS fill:#d0bfff,stroke:#333
```

## 十一、模型选型决策表

| 决策因素 | qwen2.5:3b（主） | llama3.2:1b（回退） | deepseek-v4-pro |
|---------|------------------|--------------------|--------------------|
| **中文质量** | 优秀 | 一般 | 优秀 |
| **JSON 输出** | 较好（偶需修复） | 差 | 优秀 |
| **延迟** | 3-10s | 1-3s | 10-30s（网络） |
| **上下文** | 32K | 128K | 128K |
| **显存占用** | 1.9 GB | 1.3 GB | 0（云端） |
| **成本** | 免费 | 免费 | ¥0.01/篇 |
| **适用场景** | 查询 / 摘要 | 紧急回退 | 摄取分析 |

## 相关

- [[知识库系统工程化架构]]
- [[AI 自进化知识系统 — 建设路线图]]
- [[知识库系统全链路架构]]
- [[DeepSeek API 驱动 ingest — 架构与实现]]
- [[LLM Wiki 模式]]
- [[Wiki 目录]]
